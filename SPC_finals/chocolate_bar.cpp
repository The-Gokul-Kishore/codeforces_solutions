#include<bits/stdc++.h>
using namespace std;
int main(){
    int test;
    cin>>test;
    for(int t=0;t<test;t++){
        int n,m,required;
        cin>>n>>m>>required;
        int bigcut = ceil(float(required)/n);
        if(n>m){
            bigcut ceil(float(required)/m);
        }
        
    }
}